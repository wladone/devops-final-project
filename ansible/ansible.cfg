[defaults]
inventory = inventory.ini
# Local lab only: host keys are disabled because the disposable SSH target is rebuilt often.
# Do not reuse this setting for real shared, staging, or production infrastructure.
host_key_checking = False
interpreter_python = auto_silent
timeout = 30

[ssh_connection]
pipelining = True
