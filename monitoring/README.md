# Monitoring

This folder contains a starter monitoring pack for Kubernetes-oriented operations:

- Prometheus alert rules
- Grafana dashboard definitions

The alerts assume `kube-state-metrics` is available in the cluster.
