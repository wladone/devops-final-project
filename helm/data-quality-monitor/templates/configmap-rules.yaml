apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "data-quality-monitor.fullname" . }}-rules
  labels:
    {{- include "data-quality-monitor.labels" . | nindent 4 }}
data:
  rules.yml: |
{{ .Files.Get "files/rules.yml" | indent 4 }}
